<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Favourites &mdash; Home Listings</title>
    <style>
      :root {
        --primary: #ff5a5f;
        --primary-dark: #e0484d;
        --secondary: #38b6ff;
        --accent: #FFD700;
        --bg-gradient: linear-gradient(120deg, #fff0f3 0%, #e0eafc 100%);
        --card-shadow: 0 8px 32px rgba(34, 34, 59, 0.13);
        --card-radius: 18px;
        --font-main: 'Inter', 'Segoe UI', Arial, sans-serif;
        --fav-heart: #ff5a5f;
        --fav-heart-bg: #fff0f3;
      }
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
        font-family: var(--font-main);
      }
      body {
        min-height: 100vh;
        background: var(--bg-gradient);
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        padding: 0;
      }
      header {
        width: 100%;
        background: #fff;
        box-shadow: 0 2px 12px rgba(34, 34, 59, 0.07);
        padding: 32px 0 18px 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        border-bottom-left-radius: 24px;
        border-bottom-right-radius: 24px;
        margin-bottom: 18px;
      }
      header h1 {
        font-size: 2.3rem;
        color: #22223b;
        font-weight: 700;
        letter-spacing: 1px;
        text-shadow: 0 2px 8px rgba(0,0,0,0.07);
        margin-bottom: 8px;
        display: flex;
        align-items: center;
        gap: 10px;
      }
      .fav-heart-title {
        color: var(--fav-heart);
        font-size: 1.5em;
        vertical-align: middle;
        margin-left: 2px;
      }
      nav {
        margin-top: 8px;
        display: flex;
        gap: 18px;
      }
      .nav-btn {
        text-decoration: none;
        background: linear-gradient(90deg, var(--primary) 0%, var(--secondary) 100%);
        color: #fff;
        font-size: 1.08rem;
        font-weight: 600;
        padding: 12px 28px;
        border: none;
        border-radius: 7px;
        cursor: pointer;
        box-shadow: 0 2px 8px rgba(79, 140, 255, 0.10);
        transition: background 0.2s, transform 0.15s;
        letter-spacing: 0.5px;
        display: inline-block;
      }
      .nav-btn:hover, .nav-btn:focus {
        background: linear-gradient(90deg, var(--primary-dark) 0%, #2fa4e7 100%);
        transform: translateY(-2px) scale(1.03);
      }
      main {
        width: 100%;
        max-width: 1200px;
        display: flex;
        flex-direction: column;
        align-items: center;
        margin-top: 24px;
        padding: 0 16px 40px 16px;
      }
      .section-title {
        color: var(--fav-heart);
        font-size: 2rem;
        font-weight: 700;
        margin-bottom: 18px;
        letter-spacing: 0.5px;
        text-align: center;
        text-shadow: 0 2px 8px rgba(255,90,95,0.08);
        display: flex;
        align-items: center;
        gap: 10px;
        justify-content: center;
      }
      .fav-heart {
        color: var(--fav-heart);
        font-size: 1.3em;
        margin-right: 2px;
      }
      .fav-cards {
        display: flex;
        flex-wrap: wrap;
        gap: 32px;
        justify-content: center;
        margin-top: 10px;
        width: 100%;
      }
      .fav-card {
        background: #fff;
        border-radius: var(--card-radius);
        box-shadow: var(--card-shadow);
        overflow: hidden;
        width: 340px;
        display: flex;
        flex-direction: column;
        transition: transform 0.18s, box-shadow 0.18s;
        border: 2px solid var(--fav-heart-bg);
        position: relative;
      }
      .fav-card:hover, .fav-card:focus-within {
        transform: translateY(-6px) scale(1.025);
        box-shadow: 0 12px 32px rgba(255,90,95,0.13);
        border-color: var(--fav-heart);
      }
      .fav-card-img-wrap {
        width: 100%;
        height: 190px;
        overflow: hidden;
        background: #ffe5e8;
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
      }
      .fav-card-img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
        transition: transform 0.2s;
      }
      .fav-card:hover .fav-card-img {
        transform: scale(1.04);
      }
      .fav-heart-badge {
        position: absolute;
        top: 14px;
        right: 16px;
        background: var(--fav-heart-bg);
        border-radius: 50%;
        padding: 8px;
        box-shadow: 0 2px 8px rgba(255,90,95,0.10);
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .fav-heart-badge .fa-heart {
        color: var(--fav-heart);
        font-size: 1.3em;
      }
      .fav-card-content {
        padding: 22px 20px 18px 20px;
        display: flex;
        flex-direction: column;
        gap: 10px;
      }
      .fav-card-title {
        font-size: 1.3rem;
        font-weight: 700;
        color: #22223b;
        margin: 0 0 2px 0;
        letter-spacing: 0.2px;
      }
      .fav-card-location {
        font-size: 1.05rem;
        color: #495057;
        display: flex;
        align-items: center;
        margin-bottom: 6px;
        gap: 6px;
      }
      .fav-card-details {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-top: 8px;
      }
      .fav-card-price {
        font-size: 1.15rem;
        color: var(--secondary);
        font-weight: 600;
      }
      .per-night {
        color: #6c757d;
        font-size: 0.98rem;
        font-weight: 400;
      }
      .fav-card-rating {
        font-size: 1.08rem;
        color: var(--accent);
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 2px;
      }
      .remove-fav-btn {
        margin-top: 12px;
        width: 100%;
        font-size: 1.08rem;
        padding: 10px 0;
        background: #fff0f3;
        color: var(--fav-heart);
        border: 1.5px solid var(--fav-heart);
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(255,90,95,0.07);
        cursor: pointer;
        transition: background 0.2s, color 0.2s;
        font-weight: 600;
        letter-spacing: 0.5px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 7px;
      }
      .remove-fav-btn:hover, .remove-fav-btn:focus {
        background: var(--fav-heart);
        color: #fff;
      }
      .no-favs {
        color: #6c757d;
        font-size: 1.15rem;
        margin-top: 32px;
        text-align: center;
      }
      .no-favs .fa-heart-o {
        color: var(--fav-heart);
        margin-right: 6px;
      }
      footer {
        width: 100%;
        background: #fff;
        border-top-left-radius: 24px;
        border-top-right-radius: 24px;
        box-shadow: 0 -2px 12px rgba(34, 34, 59, 0.07);
        padding: 22px 0 12px 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        margin-top: auto;
      }
      .footer-text {
        color: #adb5bd;
        font-size: 1rem;
        letter-spacing: 0.2px;
      }
      @media (max-width: 900px) {
        .fav-cards {
          gap: 20px;
        }
        .fav-card {
          width: 95vw;
          max-width: 370px;
        }
      }
      @media (max-width: 600px) {
        header h1 {
          font-size: 1.7rem;
        }
        .section-title {
          font-size: 1.2rem;
        }
        .fav-card {
          width: 98vw;
          max-width: 99vw;
        }
        main {
          padding: 0 2vw 30px 2vw;
        }
      }
    </style>
    <!-- Font Awesome CDN for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.css">
  </head>
  <body>
    <header>
      <h1>
        <span>My Favourites</span>
        <i class="fa fa-heart fav-heart-title" aria-hidden="true"></i>
      </h1>
      <nav>
        <a href="/" class="nav-btn"><i class="fa fa-home" aria-hidden="true" style="margin-right:7px;"></i>Home</a>
        <a href="/bookings" class="nav-btn"><i class="fa fa-calendar-check-o" aria-hidden="true" style="margin-right:7px;"></i>My Bookings</a>
        <a href="/host/add-home" class="nav-btn"><i class="fa fa-plus" aria-hidden="true" style="margin-right:7px;"></i>Add Home</a>
      </nav>
    </header>
    <main>
      <div class="section-title">
        <i class="fa fa-heart fav-heart" aria-hidden="true"></i>
        Your Favourite Homes
      </div>
      <% if (!registeredHomes || registeredHomes.length === 0) { %>
        <div class="no-favs">
          <i class="fa fa-heart-o" aria-hidden="true"></i>
          You haven't added any homes to your favourites yet.
          <br>
          <a href="/" style="color:var(--primary);text-decoration:underline;">Browse homes</a> and click the <i class="fa fa-heart" style="color:var(--fav-heart);"></i> to add them to your favourites!
        </div>
      <% } else { %>
      <div class="fav-cards">
        <% registeredHomes.forEach(home => { %>
          <div class="fav-card" tabindex="0">
            <div class="fav-card-img-wrap">
              <img src="<%= home.photoUrl %>" alt="Photo of <%= home.home %>" class="fav-card-img" />
              <span class="fav-heart-badge" title="Favourite">
                <i class="fa fa-heart" aria-hidden="true"></i>
              </span>
            </div>
            <div class="fav-card-content">
              <h3 class="fav-card-title"><%= home.home %></h3>
              <div class="fav-card-location">
                <i class="fa fa-map-marker" aria-hidden="true" style="color:#38b6ff;"></i>
                <%= home.location %>
              </div>
              <div class="fav-card-details">
                <span class="fav-card-price">$<%= home.price %> <span class="per-night">/ night</span></span>
                <span class="fav-card-rating">
                  <i class="fa fa-star" style="color:#FFD700;"></i>
                  <%= home.rating %>
                </span>
              </div>
              <button class="remove-fav-btn" title="Remove from favourites">
                <i class="fa fa-heart" aria-hidden="true"></i>
                Remove from Favourites
              </button>
            </div>
          </div>
        <% }) %>
      </div>
      <% } %>
    </main>
    <footer>
      <div class="footer-text">
        &copy; <%= new Date().getFullYear() %> Home Listings &mdash; Crafted with <i class="fa fa-heart" style="color:#dc3545;"></i>
      </div>
    </footer>
  </body>
</html>
