<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - Home Listings</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <!-- if you move styles to external file -->
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
          sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      /* ✅ Navbar styles */
      .navbar {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        padding: 1rem 2rem;
        box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
      }
      .navbar-list {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: center;
        list-style: none;
        margin: 0;
        padding: 0;
        gap: 0.5rem;
      }
      .navbar-item {
        flex-shrink: 0;
      }
      .nav-btn {
        background: #6366f1;
        color: white;
        border: none;
        padding: 0.75rem 1.5rem;
        border-radius: 25px;
        font-size: 0.9rem;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
      }
      .nav-btn:hover {
        background: #5855eb;
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(99, 102, 241, 0.3);
      }
      .nav-btn.active {
        background: #4338ca;
      }

      .main-container {
        flex: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
      }
      .login-card {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(20px);
        border-radius: 20px;
        padding: 2rem; /* 🔽 reduced from 3rem */
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1); /* lighter shadow */
        width: 100%;
        max-width: 380px; /* 🔽 reduced from 450px */
        border: 1px solid rgba(255, 255, 255, 0.3);
      }
      .login-header {
        text-align: center;
        margin-bottom: 1.5rem;
      }
      .login-title {
        font-size: 2rem;
        font-weight: bold;
        background: linear-gradient(135deg, #667eea, #764ba2);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 0.5rem;
      }
      .login-subtitle {
        color: #6b7280;
        font-size: 1.1rem;
      }
      .form-group {
        margin-bottom: 1rem;
      }
      .form-label {
        display: block;
        color: #374151;
        font-weight: 600;
        margin-bottom: 0.5rem;
        font-size: 0.95rem;
      }
      .form-input {
        width: 100%;
        padding: 1rem 1.25rem;
        border: 2px solid #e5e7eb;
        border-radius: 16px;
        font-size: 1rem;
        transition: all 0.3s ease;
        background: rgba(255, 255, 255, 0.8);
      }
      .form-input:focus {
        outline: none;
        border-color: #6366f1;
        background: white;
        box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
      }
      .login-button {
        width: 100%;
        background: linear-gradient(135deg, #6366f1, #8b5cf6);
        color: white;
        border: none;
        padding: 0.9rem;
        border-radius: 16px;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        margin-top: 1rem;
      }
      .login-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 15px 35px rgba(99, 102, 241, 0.4);
      }
      .login-button:active {
        transform: translateY(0);
      }
      .divider {
        text-align: center;
        margin: 1.2rem 0;
        position: relative;
        color: #9ca3af;
      }
      .divider::before {
        content: "";
        position: absolute;
        top: 50%;
        left: 0;
        right: 0;
        height: 1px;
        background: #e5e7eb;
      }
      .divider span {
        background: rgba(255, 255, 255, 0.95);
        padding: 0 1rem;
      }
      .forgot-password {
        text-align: center;
        margin-top: 1.5rem;
      }
      .forgot-password a {
        color: #6366f1;
        text-decoration: none;
        font-weight: 500;
        transition: color 0.3s ease;
      }
      .forgot-password a:hover {
        color: #5855eb;
      }
      .signup-link {
        text-align: center;
        margin-top: 1.2rem;
        padding-top: 1.2rem;
        border-top: 1px solid #e5e7eb;
        color: #6b7280;
      }
      .signup-link a {
        color: #6366f1;
        text-decoration: none;
        font-weight: 600;
      }
      .signup-link a:hover {
        color: #5855eb;
      }

      @media (max-width: 768px) {
        .navbar {
          padding: 1rem;
        }
        .nav-btn {
          padding: 0.6rem 1rem;
          font-size: 0.8rem;
        }
        .login-card {
          padding: 2rem;
          margin: 1rem;
        }
        .login-title {
          font-size: 2rem;
        }
      }

      /* Floating shapes */
      .floating-shapes {
        position: fixed;
        width: 100%;
        height: 100%;
        overflow: hidden;
        z-index: -1;
      }
      .shape {
        position: absolute;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 50%;
        animation: float 6s ease-in-out infinite;
      }
      .shape:nth-child(1) {
        width: 100px;
        height: 100px;
        top: 20%;
        left: 10%;
        animation-delay: 0s;
      }
      .shape:nth-child(2) {
        width: 150px;
        height: 150px;
        top: 60%;
        right: 10%;
        animation-delay: 2s;
      }
      .shape:nth-child(3) {
        width: 80px;
        height: 80px;
        top: 80%;
        left: 30%;
        animation-delay: 4s;
      }
      @keyframes float {
        0%,
        100% {
          transform: translateY(0px) rotate(0deg);
        }
        50% {
          transform: translateY(-20px) rotate(180deg);
        }
      }
    </style>
  </head>
  <body>
    <div class="floating-shapes">
      <div class="shape"></div>
      <div class="shape"></div>
      <div class="shape"></div>
    </div>

    <!-- ✅ Navbar -->
    <nav class="navbar">
      <ul class="navbar-list">
        <li class="navbar-item">
          <a
            href="/"
            class="nav-btn <%= activePage === 'home' ? 'active' : '' %>"
          >
            <i
              class="fa fa-home"
              aria-hidden="true"
              style="margin-right: 7px"
            ></i>
            Home
          </a>
        </li>
        <% if (isLoggedIn) { %>
        <li class="navbar-item">
          <a
            href="/homes"
            class="nav-btn <%= activePage === 'homes' ? 'active' : '' %>"
          >
            <i
              class="fa fa-list"
              aria-hidden="true"
              style="margin-right: 7px"
            ></i>
            All Homes
          </a>
        </li>
        <li class="navbar-item">
          <a
            href="/favourites"
            class="nav-btn <%= activePage === 'favourites' ? 'active' : '' %>"
          >
            <i
              class="fa fa-heart"
              aria-hidden="true"
              style="margin-right: 7px"
            ></i>
            Favourites
          </a>
        </li>
        <li class="navbar-item">
          <a
            href="/bookings"
            class="nav-btn <%= activePage === 'bookings' ? 'active' : '' %>"
          >
            <i
              class="fa fa-calendar"
              aria-hidden="true"
              style="margin-right: 7px"
            ></i>
            Bookings
          </a>
        </li>
        <li class="navbar-item">
          <a
            href="/host/add-home"
            class="nav-btn <%= activePage === 'add-home' ? 'active' : '' %>"
          >
            <i
              class="fa fa-plus"
              aria-hidden="true"
              style="margin-right: 7px"
            ></i>
            Add Home
          </a>
        </li>
        <li class="navbar-item">
          <a
            href="/host/host-home-list"
            class="nav-btn <%= activePage === 'host' ? 'active' : '' %>"
          >
            <i
              class="fa fa-user-circle"
              aria-hidden="true"
              style="margin-right: 7px"
            ></i>
            Host
          </a>
        </li>
        <% } %> <% if (!isLoggedIn) { %>

        <li class="navbar-item">
          <a
            href="/login"
            class="nav-btn <%= activePage === 'login' ? 'active' : '' %>"
          >
            <i
              class="fa fa-sign-in"
              aria-hidden="true"
              style="margin-right: 7px"
            ></i>
            Login
          </a>
        </li>
        <% } else {%>
        <form action="/logout" method="POST" style="display: inline">
          <button type="submit" class="nav-btn">
            <i
              class="fa fa-sign-out"
              aria-hidden="true"
              style="margin-right: 7px"
            ></i>
            Logout
          </button>
        </form>
        <% } %>
      </ul>
    </nav>

    <!-- ✅ Login Card -->
    <div class="main-container">
      <div class="login-card">
        <div class="login-header">
          <h1 class="login-title">Welcome Back!</h1>
          <p class="login-subtitle">Sign in to your Home Listings account</p>
        </div>

        <form action="/login" method="POST">
          <%- include('../partials/errors') %>

          <div class="form-group">
            <label for="email" class="form-label">Email Address</label>
            <input
              type="email"
              id="email"
              name="email"
              class="form-input"
              placeholder="Enter your email"
              value="<%= oldInput.email ? oldInput.email : ''%>"
              required
            />
          </div>

          <div class="form-group">
            <label for="password" class="form-label">Password</label>
            <input
              type="password"
              id="password"
              name="password"
              class="form-input"
              placeholder="Enter your password"
              required
            />
          </div>

          <% if (typeof error !== "undefined") { %>
          <p style="color: red; text-align: center; margin-bottom: 1rem">
            <%= error %>
          </p>
          <% } %>

          <button type="submit" class="login-button" value="login">
            Login to Your Account
          </button>
        </form>

        <div class="forgot-password">
          <a href="#">Forgot your password?</a>
        </div>

        <div class="divider"><span>or</span></div>

        <div class="signup-link">
          Don't have an account? <a href="/signup">Sign up here</a>
        </div>
      </div>
    </div>

    <script>
      document.querySelectorAll(".form-input").forEach((input) => {
        input.addEventListener("focus", function () {
          this.parentElement.style.transform = "translateY(-2px)";
        });
        input.addEventListener("blur", function () {
          this.parentElement.style.transform = "translateY(0)";
        });
      });

      const shapes = document.querySelectorAll(".shape");
      shapes.forEach((shape, index) => {
        shape.style.animationDelay = `${index * 2}s`;
      });
    </script>
  </body>
</html>
