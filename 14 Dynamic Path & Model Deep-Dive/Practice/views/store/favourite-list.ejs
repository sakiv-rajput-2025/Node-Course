<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Favourites &mdash; Home Listings</title>
    <style>
      :root {
        --primary: #4a6cfa;
        --primary-dark: #3d5be0;
        --secondary: #38b6ff;
        --accent: #ffb74d;
        --text-primary: #2b3445;
        --text-secondary: #626b7f;
        --bg-gradient: linear-gradient(135deg, #f5f7ff 0%, #eef8ff 100%);
        --card-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
        --card-radius: 16px;
        --font-main: 'Inter', 'Segoe UI', system-ui, sans-serif;
        --header-bg: #fff;
        --card-hover-shadow: 0 15px 40px rgba(74, 108, 250, 0.15);
        --card-hover-scale: 1.03;
      }
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
        font-family: var(--font-main);
      }
      body {
        min-height: 100vh;
        background: var(--bg-gradient);
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        padding: 0;
        color: var(--text-primary);
      }
      header {
        width: 100%;
        background: var(--header-bg);
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
        padding: 36px 0 22px 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        border-bottom-left-radius: 30px;
        border-bottom-right-radius: 30px;
        margin-bottom: 24px;
      }
      header h1 {
        font-size: 2.5rem;
        color: var(--primary);
        font-weight: 800;
        letter-spacing: -0.5px;
        text-shadow: 0 2px 10px rgba(74, 108, 250, 0.08);
        margin-bottom: 16px;
      }
      nav {
        margin-top: 12px;
        display: flex;
        gap: 16px;
      }
      .nav-btn {
        text-decoration: none;
        background: var(--primary);
        color: #fff;
        font-size: 1.05rem;
        font-weight: 600;
        padding: 14px 28px;
        border: none;
        border-radius: 12px;
        cursor: pointer;
        box-shadow: 0 4px 12px rgba(74, 108, 250, 0.18);
        transition: all 0.2s ease;
        letter-spacing: 0.2px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
      }
      .nav-btn:hover, .nav-btn:focus {
        background: var(--primary-dark);
        transform: translateY(-3px);
        box-shadow: 0 6px 16px rgba(74, 108, 250, 0.25);
      }
      main {
        width: 100%;
        max-width: 1280px;
        display: flex;
        flex-direction: column;
        align-items: center;
        margin-top: 24px;
        padding: 0 24px 60px 24px;
      }
      .section-title {
        color: var(--text-primary);
        font-size: 2.2rem;
        font-weight: 800;
        margin-bottom: 32px;
        letter-spacing: -0.5px;
        text-align: center;
        text-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
      }
      .fav-cards {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
        gap: 30px;
        width: 100%;
      }
      .fav-card {
        background: #fff;
        border-radius: var(--card-radius);
        box-shadow: var(--card-shadow);
        overflow: hidden;
        display: flex;
        flex-direction: column;
        transition: transform 0.25s ease, box-shadow 0.25s ease;
        position: relative;
        height: 100%;
      }
      .fav-card:hover, .fav-card:focus-within {
        transform: translateY(-6px) scale(var(--card-hover-scale));
        box-shadow: var(--card-hover-shadow);
      }
      .fav-card-img-wrap {
        width: 100%;
        height: 220px;
        overflow: hidden;
        background: #f8f9fa;
        position: relative;
      }
      .fav-card-img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
        transition: transform 0.3s ease;
      }
      .fav-card:hover .fav-card-img {
        transform: scale(1.05);
      }
      .fav-card-content {
        padding: 24px 20px;
        display: flex;
        flex-direction: column;
        gap: 12px;
        flex-grow: 1;
      }
      .fav-card-title {
        font-size: 1.4rem;
        font-weight: 700;
        color: var(--text-primary);
        letter-spacing: -0.3px;
        line-height: 1.3;
      }
      .fav-card-location {
        font-size: 1.05rem;
        color: var(--text-secondary);
        display: flex;
        align-items: center;
        gap: 6px;
        margin-bottom: 4px;
      }
      .fav-card-details {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-top: 12px;
        padding-top: 12px;
        border-top: 1px solid rgba(0,0,0,0.06);
      }
      .fav-card-price {
        font-size: 1.25rem;
        color: var(--primary);
        font-weight: 700;
      }
      .per-night {
        color: var(--text-secondary);
        font-size: 0.95rem;
        font-weight: 500;
      }
      .fav-card-rating {
        font-size: 1.1rem;
        color: var(--text-primary);
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 5px;
        background: rgba(255, 183, 77, 0.1);
        padding: 6px 12px;
        border-radius: 50px;
      }
      .no-favs {
        color: var(--text-secondary);
        font-size: 1.2rem;
        margin-top: 48px;
        text-align: center;
        line-height: 1.6;
      }
      footer {
        width: 100%;
        background: var(--header-bg);
        border-top-left-radius: 30px;
        border-top-right-radius: 30px;
        box-shadow: 0 -4px 20px rgba(0, 0, 0, 0.06);
        padding: 30px 0 20px 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        margin-top: auto;
      }
      .footer-text {
        color: var(--text-secondary);
        font-size: 1rem;
        letter-spacing: 0.2px;
      }
      @media (max-width: 900px) {
        .fav-cards {
          grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
          gap: 24px;
        }
      }
      @media (max-width: 640px) {
        header {
          padding: 28px 0 18px 0;
        }
        header h1 {
          font-size: 2rem;
        }
        .section-title {
          font-size: 1.6rem;
          margin-bottom: 24px;
        }
        .nav-btn {
          padding: 12px 18px;
          font-size: 0.95rem;
        }
        nav {
          gap: 10px;
        }
        .fav-cards {
          grid-template-columns: 1fr;
        }
        main {
          padding: 0 16px 40px 16px;
        }
      }
    </style>
    <!-- Font Awesome CDN for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.css">
  </head>
  <body>
    <header>
      <h1>My Favourites</h1>
      <nav>
        <a href="/" class="nav-btn"><i class="fa fa-home" aria-hidden="true" style="margin-right:8px;"></i>Home</a>
        <a href="/bookings" class="nav-btn"><i class="fa fa-calendar-check-o" aria-hidden="true" style="margin-right:8px;"></i>My Bookings</a>
        <a href="javascript:history.back()" class="nav-btn"><i class="fa fa-arrow-left" aria-hidden="true" style="margin-right:8px;"></i>Back</a>
      </nav>
    </header>
    <main>
      <div class="section-title">
        Your Favourite Homes
      </div>
      <% if (!favouriteHomes || favouriteHomes.length === 0) { %>
        <div class="no-favs">
          You haven't added any homes to your favourites yet.
          <br>
          <a href="/" style="color:var(--primary);text-decoration:underline;font-weight:500;">Browse homes</a> to add them to your favourites!
        </div>
      <% } else { %>
      <div class="fav-cards">
        <% favouriteHomes.forEach(home => { %>
          <div class="fav-card" tabindex="0">
            <div class="fav-card-img-wrap">
              <img src="<%= home.photoUrl %>" alt="Photo of <%= home.home %>" class="fav-card-img" />
            </div>
            <div class="fav-card-content">
              <h3 class="fav-card-title"><%= home.home %></h3>
              <div class="fav-card-location">
                <i class="fa fa-map-marker" aria-hidden="true" style="color:var(--secondary);"></i>
                <%= home.location %>
              </div>
              <div class="fav-card-details">
                <span class="fav-card-price">$<%= home.price %> <span class="per-night">/ night</span></span>
                <span class="fav-card-rating">
                  <i class="fa fa-star" style="color:var(--accent);"></i>
                  <%= home.rating %>
                </span>
              </div>
              <form action="/favourites/delete/<%= home.id %>" method="POST" style="margin-top:12px;">
                <button type="submit" class="nav-btn" style="background:var(--danger);color:#fff;border:none;padding:8px 16px;border-radius:6px;cursor:pointer;display:inline-flex;align-items:center;gap:6px;">
                  <i class="fa fa-heart" aria-hidden="true" style="color:#fff;"></i>
                  Remove
                </button>
              </form>
            </div>
          </div>
        <% }) %>
      </div>
      <% } %>
    </main>
    <footer>
      <div class="footer-text">
        &copy; <%= new Date().getFullYear() %> Home Listings &mdash; All rights reserved
      </div>
    </footer>
  </body>
</html>