<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Bookings</title>
    <style>
      :root {
        --primary: #38b6ff;
        --accent: #ffb347;
        --bg: #f7faff;
        --card-bg: #fff;
        --text: #22223b;
        --muted: #6c757d;
        --border: #e0e0e0;
      }
      body {
        background: var(--bg);
        font-family: "Segoe UI", Arial, sans-serif;
        color: var(--text);
        margin: 0;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }
      header {
        background: var(--primary);
        color: #fff;
        padding: 24px 0 16px 0;
        text-align: center;
        box-shadow: 0 2px 8px rgba(34, 34, 59, 0.07);
      }
      header h1 {
        margin: 0 0 8px 0;
        font-size: 2.1rem;
        letter-spacing: 1px;
      }
      nav {
        margin-top: 8px;
      }
      .nav-btn {
        display: inline-block;
        background: #fff;
        color: var(--primary);
        border: none;
        border-radius: 7px;
        padding: 8px 18px;
        margin: 0 7px;
        font-weight: 600;
        font-size: 1rem;
        text-decoration: none;
        box-shadow: 0 2px 8px rgba(34, 34, 59, 0.07);
        transition: background 0.2s, color 0.2s;
      }
      .nav-btn:hover {
        background: var(--accent);
        color: #fff;
      }
      main {
        flex: 1;
        padding: 32px 2vw 30px 2vw;
        max-width: 900px;
        margin: 0 auto;
      }
      .section-title {
        font-size: 1.5rem;
        font-weight: 700;
        margin-bottom: 24px;
        color: var(--primary);
        letter-spacing: 0.5px;
      }
      .bookings-list {
        display: flex;
        flex-direction: column;
        gap: 24px;
      }
      .booking-card {
        background: var(--card-bg);
        border-radius: 12px;
        box-shadow: 0 2px 8px rgba(34, 34, 59, 0.07);
        display: flex;
        align-items: flex-start;
        padding: 20px;
        border: 1px solid var(--border);
        gap: 22px;
      }
      .booking-img-wrap {
        flex-shrink: 0;
        width: 120px;
        height: 90px;
        border-radius: 8px;
        overflow: hidden;
        background: #e9ecef;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .booking-img {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }
      .booking-details {
        flex: 1;
        display: flex;
        flex-direction: column;
        justify-content: center;
      }
      .booking-home {
        font-size: 1.18rem;
        font-weight: 600;
        margin-bottom: 6px;
        color: var(--text);
      }
      .booking-location {
        color: var(--muted);
        font-size: 1rem;
        margin-bottom: 10px;
        display: flex;
        align-items: center;
        gap: 5px;
      }
      .booking-rating-price {
        display: flex;
        align-items: center;
        gap: 18px;
        margin-bottom: 6px;
      }
      .booking-rating {
        color: #ffd700;
        font-size: 1rem;
        display: flex;
        align-items: center;
        gap: 4px;
      }
      .booking-price {
        color: var(--primary);
        font-size: 1.08rem;
        font-weight: 600;
      }
      .no-details {
        text-align: center;
        color: #dc3545;
        font-size: 1.2rem;
        margin-top: 40px;
        font-weight: 600;
      }
      footer {
        background: #fff;
        color: var(--muted);
        text-align: center;
        padding: 18px 0 10px 0;
        font-size: 1rem;
        border-top: 1px solid var(--border);
      }
      @media (max-width: 800px) {
        main {
          padding: 18px 1vw 20px 1vw;
        }
        .booking-card {
          padding: 12px;
          gap: 14px;
        }
        .booking-img-wrap {
          width: 90px;
          height: 70px;
        }
      }
      @media (max-width: 600px) {
        .booking-card {
          flex-direction: column;
          align-items: stretch;
          padding: 10px;
          gap: 10px;
        }
        .booking-img-wrap {
          width: 100%;
          height: 150px;
          margin-bottom: 10px;
        }
        .booking-details {
          align-items: flex-start;
        }
      }
    </style>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.css"
    />
  </head>
  <body>
    <header>
      <h1>My Bookings</h1>
      <nav>
        <a href="/" class="nav-btn"
          ><i
            class="fa fa-home"
            aria-hidden="true"
            style="margin-right: 7px"
          ></i
          >Home</a
        >
        <a href="/" class="nav-btn"
          ><i
            class="fa fa-list"
            aria-hidden="true"
            style="margin-right: 7px"
          ></i
          >Browse Homes</a
        >
      </nav>
    </header>
    <main>
      <div class="section-title">Your Booked Homes</div>
      <% if (!bookings || bookings.length === 0) { %>
      <div class="no-details">
        <i
          class="fa fa-eye-slash"
          aria-hidden="true"
          style="color: #dc3545; margin-right: 6px"
        ></i>
        No bookings to show.
      </div>
      <% } else { %>
      <div class="bookings-list">
        <% bookings.forEach(booking => { %>
        <div class="booking-card">
          <div class="booking-img-wrap">
            <% if (booking.photoUrl) { %>
            <img
              src="<%= booking.photoUrl %>"
              alt="Photo of <%= booking.houseName ? booking.houseName : 'Home' %>"
              class="booking-img"
            />
            <% } else { %>
            <span style="color: var(--muted); font-size: 1.2rem">No Image</span>
            <% } %>
          </div>
          <div class="booking-details">
            <div class="booking-home">
              <%= booking.houseName ? booking.houseName : 'No Home Name' %>
            </div>
            <div class="booking-location">
              <i
                class="fa fa-map-marker"
                aria-hidden="true"
                style="color: #38b6ff"
              ></i>
              <%= booking.location ? booking.location : 'No Location' %>
            </div>
            <div class="booking-rating-price">
              <span class="booking-rating">
                <i class="fa fa-star" aria-hidden="true"></i>
                <%= booking.rating ? booking.rating : 'N/A' %>
              </span>
              <span class="booking-price">
                $<%= booking.price ? booking.price : 'N/A' %>
                <span
                  style="
                    font-size: 0.95em;
                    font-weight: 400;
                    color: var(--muted);
                  "
                  >/ night</span
                >
              </span>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
      <% } %>
    </main>
    <footer>
      <div class="footer-text">
        &copy; <%= new Date().getFullYear() %> Home Listings &mdash; Crafted
        with <i class="fa fa-heart" style="color: #dc3545"></i>
      </div>
    </footer>
  </body>
</html>
